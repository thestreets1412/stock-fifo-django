Plain bordered card (1px gray border, 0.5rem radius, no shadow) — used for the login box and the evidence-image viewer.

```jsx
<Card style={{ maxWidth: 400 }}>
  <CardBody>
    <CardTitle>Login</CardTitle>
    ...
  </CardBody>
</Card>
```
