Outline-secondary button + menu, used for the "FIFO Report" export trigger (Download CSV / Download PDF).

```jsx
<Dropdown label="FIFO Report" items={[{label:"Download CSV"}, {label:"Download PDF"}]} />
```
