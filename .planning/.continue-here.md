---
context: default
phase: (milestone-level — work ran through docs/superpowers/plans/, not .planning/phases/)
task: 7
total_tasks: 7
status: complete
last_updated: 2026-08-08T07:24:25.580Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure or near-miss this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **`.planning/STATE.md` and `.planning/ROADMAP.md` are stale and actively misleading.**
      They say Phase 2 is "Ready to plan / Not started" as of 2026-07-19, and Phases 3–6 "Not started."
      In reality Phases 2, 3, 4 and 5 are **done and running in production**, implemented via
      `docs/superpowers/plans/` on the `bootstrap-fw` track rather than through GSD phase directories.
      — Structural mitigation: **do not** run `/gsd-plan-phase` or `/gsd-execute-phase` off these files.
      Reconcile them against `git log main` and `README.md` § *Public access* first. Re-executing
      "Phase 2: Gunicorn + systemd" would overwrite a working, verified production unit file.

- [ ] CONSTRAINT: **A clean `git merge` does not mean a semantically correct merge.**
      `main` + `bootstrap-fw` auto-merged with zero reported conflicts, yet produced **two competing
      security blocks** in `config/settings.py` — the DEBUG-gated one from `main` and a hardcoded one
      from `bootstrap-fw`, with the hardcoded one winning by source order.
      — Structural mitigation: after any merge touching `config/settings.py`, run
      `grep -c 'SESSION_COOKIE_SECURE' config/settings.py` (must be exactly `1`) and
      `python manage.py test portfolio.tests_deploy` before committing.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting GSD state files as ground truth | `STATE.md` describes a world three weeks stale because the work happened on a different track | blocking | Cross-check `git log main` and `README.md` before planning; treat `.planning/` as a claim, not a record |
| Clean-merge complacency | Two branches edited different regions of the same file; git saw no conflict, the result had duplicate contradictory config | blocking | Post-merge assertion greps + `portfolio/tests_deploy.py` before commit |
| Conflict markers surviving into a commit | `.gitignore` carried `<<<<<<< HEAD` committed to `main` since `a7a806e` (2026-07-23); nobody touched the file again, so today's merge silently forwarded it as "clean" | advisory | `grep -rnE '^(<<<<<<<\|=======\|>>>>>>>)' -- . ` before any merge commit |
| Reading `curl` exit noise as an app bug | `curl` returned `000` immediately after `systemctl restart` — a race, not a failure; `000` is a curl code meaning "could not connect", not an HTTP status | advisory | Insert `sleep 2` after service restart before probing |

<current_state>
Milestone **complete and verified in production**. Nothing is in flight, no branch is half-merged,
no service is in a partial state.

`https://fifo-by-minotaur.uk` serves the app through a Cloudflare Tunnel, behind a Cloudflare Access
one-time-PIN gate restricted to a single email address. Verified from mobile data after a full
`sudo reboot` with no SSH intervention.

`main` is the single deployable branch and the Pi tracks it. Working tree clean at `5460cdb`.
</current_state>

<completed_work>

Seven tasks from `docs/superpowers/plans/2026-08-08-domain-tunnel-go-live.md`, all done:

- Task 1: Recon the Pi (read-only) — Done
- Task 2: Reconcile `main` and `bootstrap-fw` — Done (`40779d8`, `e495618`)
- Task 3: Tunnel config into the repo — Done (`4ba5730`)
- Task 4: Move the Pi onto merged `main` — Done
- Task 5: Create the tunnel, prove it in foreground — Done
- Task 6: systemd service + Cloudflare Access + edge TLS — Done
- Task 7: Reboot survival, external verification, docs — Done (`85e0040`, `5460cdb`)

Commits on `main` this session:

| Commit | What |
|---|---|
| `40779d8` | auto-merge of `bootstrap-fw` |
| `e495618` | merge resolution — single DEBUG-gated security block in `config/settings.py` |
| `4ba5730` | `deploy/cloudflared/` config-as-code, `.env.example` with real domain, `/media/` todo |
| `85e0040` | removed `.gitignore` conflict markers left by the 2026-07-23 merge |
| `5460cdb` | README § *Public access* |

Rollback point: tag `pre-merge-2026-08-08`.
</completed_work>

<remaining_work>

Nothing blocking. Tracked follow-ups, none of which gate anything:

- `/media/` has no auth check of its own — `.planning/todos/pending/unauthenticated-media-endpoint.md`.
  Currently mitigated only at the Cloudflare edge. Fix is to route templates at the existing
  owner-scoped `LotEvidenceView` / `SaleEvidenceView` and delete the `re_path` in `config/urls.py`.
- No off-site backup copy; no alert when a backup fails (both pre-existing, in README § *Known gaps*).
- Cloudflare edge hardening beyond TLS (WAF, rate limiting) — ROADMAP Phase 6. Defence in depth only;
  Access already blocks unauthenticated traffic.
- `.local` mDNS still fails on the Win11 laptop — now affects only Samba-by-name, not the app.
- **Reconcile the two planning systems** — see the blocking constraint above.
</remaining_work>

<decisions_made>

- Kept `main`'s DEBUG-gated security block, dropped `bootstrap-fw`'s hardcoded duplicate; kept
  `bootstrap-fw`'s WhiteNoise, Gunicorn pin, vendored assets, backup scripts and Samba config.
  Neither branch alone was deployable.
- Apex hostname only. CNAME flattening handles it; one hostname means one ingress rule, one CSRF
  origin, one Access application.
- Access via one-time PIN, single-address allowlist — no identity provider to configure or debug,
  and a blank application path is what puts `/media/` behind the gate.
- Dropped plain-HTTP LAN access on purpose. An env escape hatch would put session cookies in the
  clear on the home WiFi, and the domain already works on the home network.
- Tunnel ingress lives in the repo (`deploy/cloudflared/config.yml.example`), matching the existing
  `deploy/samba/nas-share.conf` pattern. The Pi is never the source of truth.
- Gunicorn gained `--threads 4 --timeout 60` plus access logging — the unit had no timeout, so the
  30s default could kill a worker during a slow yfinance price fetch and return 502.
- Deleted `origin/develop` and `origin/installation` (both 0 ahead of `bootstrap-fw`).
- HSTS is emitted by Django only, never also by the Cloudflare dashboard.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `README.md` § *Public access* — the authoritative description of how the app is now reached.
2. `docs/superpowers/plans/2026-08-08-domain-tunnel-go-live.md` — the executed plan, with the
   pre-flight branch analysis that explains why the merge was shaped the way it was.
3. `deploy/cloudflared/README.md` — how to change tunnel ingress without hand-editing the Pi.
4. `.planning/todos/pending/unauthenticated-media-endpoint.md` — the one real security gap left.
5. `.planning/ROADMAP.md` and `.planning/STATE.md` — **read as stale claims, not as record.**

## Infrastructure State

- **Pi** (`stockfifo`, `10.15.30.131`): on `main` at `5460cdb`, Python 3.13.5 in
  `/home/minotaur/stock-fifo-django/.venv`.
- **`stock-fifo.service`**: enabled, running. Gunicorn `127.0.0.1:8000`, 2 workers, 4 threads,
  60s timeout. The loopback-only bind is what makes trusting `X-Forwarded-Proto` sound —
  **never widen it.**
- **`cloudflared.service`**: enabled, running, `--config /etc/cloudflared/config.yml`.
  Named tunnel `stock-fifo`, UUID `2c9751e5…704e` — read the full value on the Pi with
  `cloudflared tunnel list` or `sudo grep '^tunnel:' /etc/cloudflared/config.yml`.
  Credentials at `/etc/cloudflared/<uuid>.json`, mode `600` — secret, never commit.
- **Cloudflare**: apex proxied CNAME to the tunnel; SSL/TLS mode `Full`; Always Use HTTPS on;
  Minimum TLS 1.2; dashboard HSTS deliberately **off**. Access application covers the whole
  hostname with a blank path, session 24h, one-time PIN, single-email allow policy.
- **`.env` on the Pi** (not in git): `DEBUG=False`,
  `ALLOWED_HOSTS=127.0.0.1,localhost,fifo-by-minotaur.uk`,
  `CSRF_TRUSTED_ORIGINS=https://fifo-by-minotaur.uk`.
- **Backups**: `stock-fifo-backup.timer` active, nightly 20:00. Three tiers (SD 14d / USB 60d /
  laptop pull). USB tier confirmed populated this session — the open question in
  `.planning/research/questions.md` #2 is answered **yes**.
- **Samba**: unchanged, LAN-only, bound to `127.0.0.1` and the LAN IP. Verified unreachable
  through the tunnel (`Test-NetConnection … -Port 445` → `TcpTestSucceeded: False`).

<context>
The tunnel work itself was routine. The session's real content was the branch archaeology in Task 2:
two planning systems had each built half of the same milestone, and no single branch was deployable.
Everything downstream depended on getting that merge semantically right, which is why the merge is
guarded by assertion greps and `portfolio/tests_deploy.py` rather than by eyeballing the diff.

The Pi is driven entirely by hand — there is no SSH access from the agent side, so every device-side
step was handed over as a copy-paste block with a stated expected output, and the output was read
back before moving on. Keep doing that; it is what caught the stale `.trycloudflare.com` entry and
the missing `CSRF_TRUSTED_ORIGINS` line in `.env`.
</context>

<next_action>
Nothing is in flight — this is a clean stopping point, not a pause mid-task.

If the next session intends to plan new work, start with: reconcile `.planning/STATE.md` and
`.planning/ROADMAP.md` against `git log main` and `README.md`, marking Phases 2–5 complete and
recording that they were delivered via `docs/superpowers/plans/`. Do that **before** invoking any
`/gsd-*` planning command, or the stale state will drive a redo of working production config.
</next_action>
