---
phase: 02-gunicorn-systemd-process-supervision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config/settings.py
  - config/gunicorn_conf.py
autonomous: true
requirements:
  - PROC-01
  - PROC-03
must_haves:
  truths:
    # --- Locally verifiable on this Windows dev machine (automated) ---
    - "requirements.txt pins gunicorn==26.0.0 and whitenoise==6.12.0, alphabetically ordered, with == exact pins, and `pip install -r requirements.txt` succeeds in the venv"
    - "WhiteNoiseMiddleware is the second entry in MIDDLEWARE (index 1), immediately after SecurityMiddleware"
    - "STORAGES defines both keys: default -> django.core.files.storage.FileSystemStorage (media unchanged) and staticfiles -> whitenoise.storage.CompressedManifestStaticFilesStorage"
    - "`python manage.py check` reports no issues with the new middleware and STORAGES wiring"
    - "`python manage.py collectstatic --noinput` succeeds and produces staticfiles/staticfiles.json (manifest) plus at least one .gz compressed asset, proving the whitenoise CompressedManifest backend runs at build time"
    - "config/gunicorn_conf.py compiles (py_compile) and defines bind == '127.0.0.1:8000' exactly, worker_class == 'sync', workers >= 1, and timeout >= 60 (headroom for the sequential price-fetch dashboard loop, RESEARCH Pitfall 3)"
    # --- Pi-only: gunicorn cannot run on this Windows machine (no fcntl, no WSL) — confirmed in Plan 02's checkpoint ---
    - statement: "gunicorn actually starts and serves the Django app bound to 127.0.0.1 only, and is NOT reachable from any other LAN device (loopback boundary confirmed live on the Pi)"
      verification: backstop
    - statement: "static assets load correctly (styled page, zero /static/ 404s) when served by whitenoise from within the running gunicorn process on the Pi"
      verification: backstop
    - statement: "multiple gunicorn sync workers serving the same static file concurrently is safe: compression happens once at collectstatic build-time (single-threaded), and workers only READ pre-compressed immutable files at runtime — no runtime write contention (answers the PROC-03 concurrency probe)"
      verification: backstop
    - statement: "pip install -r requirements.txt on the Pi (aarch64) resolves prebuilt wheels for gunicorn, whitenoise, and every pinned compiled dep (pillow/numpy/pandas/curl_cffi/etc.) with no compiler invoked (RESEARCH Pitfall 4 dry-run)"
      verification: backstop
  artifacts:
    - requirements.txt (gunicorn==26.0.0, whitenoise==6.12.0 added)
    - config/settings.py (WhiteNoiseMiddleware inserted at index 1; STORAGES dict added)
    - config/gunicorn_conf.py (NEW — bind/workers/worker_class/timeout/logging)
  key_links:
    - "WhiteNoiseMiddleware ordered immediately after SecurityMiddleware — it short-circuits static requests before session/CSRF/auth middleware run"
    - "STORAGES['staticfiles'] backend -> collectstatic manifest (staticfiles.json) -> whitenoise serves hashed, compressed, cache-busted assets"
    - "gunicorn_conf.py bind='127.0.0.1:8000' is the loopback trust boundary for this whole milestone until Phase 5's tunnel exists"
  prohibitions:
    - "MUST NOT bind gunicorn to a non-loopback interface (all-interfaces address or a LAN IP). The app has no tunnel/Access layer yet; exposing it off-loopback reopens the CSRF/header-forgery risk the Phase 1 settings.py comment (T-01-01) explicitly calls out."
    - "MUST NOT introduce a second static-file server or reverse proxy (nginx/Caddy) to serve /static/. Whitenoise serves from within gunicorn — the 'no local reverse proxy' decision is locked in PROJECT.md."
    - "MUST NOT modify media serving or config/urls.py media routing. That is Phase 3 (SEC-01) scope; the historical Pi commit 091d58c was a media-routing fix, unrelated to process supervision (RESEARCH Pitfall 5)."
  flagged_assumptions:
    - "PROC-01 'precision' edge probe row is NOT MEANINGFULLY APPLICABLE: a bind-address string has no numeric precision/rounding/overflow concept. Surfaced, not force-fit into a criterion."
    - "RESOLVED (was RESEARCH Open Question 1 / Assumption A2): gunicorn CANNOT be executed on this Windows dev machine — `fcntl` is unavailable (ModuleNotFoundError) and no WSL distro is installed. There is no local Linux fallback. All gunicorn/systemd runtime behavior is therefore Pi-only human_verification, not automated."
---

<objective>
Convert the app's static-serving and WSGI-serving configuration from `manage.py runserver` defaults to a production posture: add gunicorn + whitenoise as pinned dependencies, wire WhiteNoiseMiddleware and the Django 6 STORAGES dict so static files are served (compressed + cache-busted) from inside the app process, and author a gunicorn config module that binds to loopback only with a worker timeout sized for this app's real dashboard latency.

Purpose: PROC-01 (gunicorn bound to 127.0.0.1) and PROC-03 (whitenoise static from within the process) both require this application-layer configuration to exist and be correct before the systemd unit (Plan 02) can supervise it or the Pi can verify it end-to-end.
Output: requirements.txt (2 deps added), config/settings.py (middleware + STORAGES), config/gunicorn_conf.py (NEW).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gunicorn-systemd-process-supervision/02-RESEARCH.md
@.planning/phases/02-gunicorn-systemd-process-supervision/02-PATTERNS.md
@config/settings.py
@requirements.txt
@portfolio/services.py
</context>

## Artifacts this phase produces (whole phase, both plans)

| Artifact | Plan | New/Modified |
|----------|------|--------------|
| `requirements.txt` (+ `gunicorn==26.0.0`, `whitenoise==6.12.0`) | 02-01 | Modified |
| `config/settings.py` (WhiteNoiseMiddleware at index 1; `STORAGES` dict) | 02-01 | Modified |
| `config/gunicorn_conf.py` (bind/workers/timeout/logging) | 02-01 | New |
| `deploy/systemd/gunicorn.service` (systemd unit) | 02-02 | New |

## Windows-vs-Pi verification split (READ BEFORE EXECUTING)

Confirmed at plan time: this Windows dev machine cannot run gunicorn (`import fcntl` fails; gunicorn depends on `fcntl`/`os.fork`) and has **no WSL distro** installed. Therefore:

- **Automated `<verify>` here (Windows):** dependency pinning + install, settings/middleware/STORAGES wiring, `manage.py check`, `collectstatic` (this exercises the whitenoise storage backend without needing gunicorn), and `gunicorn_conf.py` syntax + values.
- **Pi-only (`human_verification` in Plan 02):** gunicorn actually starting/serving, static loading under gunicorn in a browser, loopback-only reachability confirmed live, and the aarch64 install dry-run. Do NOT write a `<verify>` here that claims to have run gunicorn.

<tasks>

<task type="auto">
  <name>Task 1: Pin gunicorn and whitenoise in requirements.txt and install into the venv</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (existing pin format: alphabetical, `==` exact pins)
    - .planning/phases/02-gunicorn-systemd-process-supervision/02-RESEARCH.md — "Standard Stack" (versions) and "Package Legitimacy Audit" (both packages are approved false-positive SUS; no checkpoint required before install)
    - .planning/phases/02-gunicorn-systemd-process-supervision/02-PATTERNS.md — requirements.txt insertion pattern
  </read_first>
  <action>
    Add exactly two pinned entries to requirements.txt, preserving the existing alphabetical order and `==` convention: `gunicorn==26.0.0` (insert after the `Django==6.0.7` line) and `whitenoise==6.12.0` (insert after the `websockets==16.1` line). Do not reorder, upgrade, or remove any existing pin. Then install into the project venv so downstream verifies (Task 2's `manage.py check`) can import whitenoise. Note for the record: both packages were flagged SUS by the legitimacy checker only because PyPI download telemetry is unavailable; RESEARCH cross-verified both as legitimate and explicitly approved them, so no human-verify checkpoint gates this install. gunicorn installs cleanly on Windows (pure-Python wheel); it simply cannot be RUN here — that is expected and handled by the Pi checkpoint in Plan 02.
  </action>
  <verify>
    <automated>Run `.venv/Scripts/python.exe -m pip install -r requirements.txt` (exit 0), then `.venv/Scripts/python.exe -m pip show gunicorn` shows Version: 26.0.0 and `.venv/Scripts/python.exe -m pip show whitenoise` shows Version: 6.12.0. Confirm requirements.txt contains the two lines `gunicorn==26.0.0` and `whitenoise==6.12.0`.</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains `gunicorn==26.0.0` and `whitenoise==6.12.0`, each placed in correct alphabetical position, with no other line changed
    - Both packages install successfully into the venv (versions confirmed by `pip show`)
  </acceptance_criteria>
  <done>requirements.txt pins both deps in alphabetical order; `pip install -r requirements.txt` succeeds and `pip show` confirms gunicorn 26.0.0 + whitenoise 6.12.0.</done>
</task>

<task type="auto">
  <name>Task 2: Wire WhiteNoiseMiddleware and the STORAGES dict in settings.py</name>
  <files>config/settings.py</files>
  <read_first>
    - config/settings.py:61-69 (current MIDDLEWARE list — 7 entries) and :133-143 (STATIC_URL / STATIC_ROOT / MEDIA_URL / MEDIA_ROOT)
    - .planning/phases/02-gunicorn-systemd-process-supervision/02-PATTERNS.md — MIDDLEWARE insertion pattern (index 1) and Django 6 STORAGES pattern
    - .planning/phases/02-gunicorn-systemd-process-supervision/02-RESEARCH.md — Pattern 2 (middleware placement) and Pitfall 2 (collectstatic requirement)
  </read_first>
  <action>
    In config/settings.py, insert `'whitenoise.middleware.WhiteNoiseMiddleware'` into the MIDDLEWARE list as the SECOND entry (index 1), immediately after `'django.middleware.security.SecurityMiddleware'` and before `'django.contrib.sessions.middleware.SessionMiddleware'` — ordering is load-bearing because whitenoise short-circuits static requests before session/CSRF/auth middleware. After the STATIC_ROOT line (~line 137), add a `STORAGES` dict with two keys: `"default"` -> BACKEND `"django.core.files.storage.FileSystemStorage"` (preserves existing media-upload behavior, untouched by this phase) and `"staticfiles"` -> BACKEND `"whitenoise.storage.CompressedManifestStaticFilesStorage"`. Use the Django 6 STORAGES dict form; do NOT use the deprecated single-string `STATICFILES_STORAGE`. Do not change STATIC_URL, STATIC_ROOT, MEDIA_URL, MEDIA_ROOT, or any Phase 1 security setting. Add a brief inline comment noting whitenoise serves `/static/` from within the process (no second server) and that `collectstatic` must be run before serving.
  </action>
  <verify>
    <automated>1) `.venv/Scripts/python.exe manage.py check` reports no issues. 2) `.venv/Scripts/python.exe -c "import os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings');import django;django.setup();from django.conf import settings as s;assert s.MIDDLEWARE[1]=='whitenoise.middleware.WhiteNoiseMiddleware',s.MIDDLEWARE;assert s.STORAGES['staticfiles']['BACKEND']=='whitenoise.storage.CompressedManifestStaticFilesStorage';assert s.STORAGES['default']['BACKEND']=='django.core.files.storage.FileSystemStorage';print('OK')"` prints OK. 3) `.venv/Scripts/python.exe manage.py collectstatic --noinput` exits 0 and `staticfiles/staticfiles.json` exists with at least one `.gz` file under `staticfiles/`.</automated>
  </verify>
  <acceptance_criteria>
    - WhiteNoiseMiddleware is at MIDDLEWARE index 1; total MIDDLEWARE length is 8
    - STORAGES has both `default` (FileSystemStorage) and `staticfiles` (whitenoise CompressedManifestStaticFilesStorage) keys
    - `manage.py check` passes; `collectstatic --noinput` produces `staticfiles/staticfiles.json` + at least one compressed `.gz` asset
    - No change to STATIC_URL/STATIC_ROOT/MEDIA_*/Phase-1 security settings
  </acceptance_criteria>
  <done>WhiteNoiseMiddleware is index 1; STORAGES wires both backends; `manage.py check` passes; `collectstatic --noinput` builds a compressed manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Author config/gunicorn_conf.py (loopback bind, sized timeout)</name>
  <files>config/gunicorn_conf.py</files>
  <read_first>
    - config/wsgi.py (unchanged — exposes `application`, gunicorn's target)
    - portfolio/services.py:108-190 (PRICE_FETCH_TIMEOUT_SECONDS=5; sequential per-symbol price fetch — justifies timeout headroom)
    - .planning/phases/02-gunicorn-systemd-process-supervision/02-RESEARCH.md — Pattern 1 (direct-bind, no socket activation) and Pitfall 3 (default 30s timeout too short)
    - .planning/phases/02-gunicorn-systemd-process-supervision/02-PATTERNS.md — gunicorn_conf.py module structure (Python top-level assignments)
  </read_first>
  <action>
    Create config/gunicorn_conf.py as a plain Python module of top-level variable assignments (gunicorn reads module-level names via `--config`). Set: `bind = "127.0.0.1:8000"` (loopback ONLY — this is the milestone's trust boundary until Phase 5; describe it in a comment as loopback-only / never exposed beyond the local interface, worded conceptually without embedding an all-interfaces literal); `workers = 2` (single-user app on a Pi 4; RESEARCH A3 — trivially tunable later); `worker_class = "sync"` (no async views in this app); `timeout = 60` (RESEARCH Pitfall 3: `build_dashboard_summary()` fetches each symbol's price sequentially at up to 5s each, so a 7+ symbol portfolio can exceed gunicorn's default 30s and get the worker SIGKILLed mid-request — 60s gives graceful-degradation headroom); `accesslog = "-"` and `errorlog = "-"` (stdout/stderr, captured by systemd/journald — no separate log files). Add a module docstring noting it is loaded via `ExecStart --config` in gunicorn.service and that timeout is tunable if a larger portfolio still trips it (RESEARCH A4). Do not add function/class definitions.
  </action>
  <verify>
    <automated>1) `.venv/Scripts/python.exe -m py_compile config/gunicorn_conf.py` exits 0. 2) `.venv/Scripts/python.exe -c "import config.gunicorn_conf as g;assert g.bind=='127.0.0.1:8000',g.bind;assert g.worker_class=='sync';assert g.workers>=1;assert g.timeout>=60;print('OK')"` prints OK.</automated>
  </verify>
  <acceptance_criteria>
    - config/gunicorn_conf.py compiles and imports cleanly
    - `bind` is EXACTLY `127.0.0.1:8000` (loopback); `worker_class == "sync"`; `workers >= 1`; `timeout >= 60`
    - accesslog/errorlog route to stdout/stderr (`-`)
    - No fenced-code or all-interfaces literal in the file; the bind value is asserted positively (not via a negative grep)
  </acceptance_criteria>
  <done>config/gunicorn_conf.py compiles; `bind == '127.0.0.1:8000'`, `worker_class == 'sync'`, `workers >= 1`, `timeout >= 60`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loopback → app | Requests reach the app only via `127.0.0.1:8000`. No tunnel/LAN exposure exists in this phase; loopback is the sole ingress and the enforced trust boundary. |
| build-time → run-time (static) | Static assets are compressed + hashed once at `collectstatic` time; the running process only reads immutable pre-built files. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure / Elevation of Privilege | gunicorn bind address (config/gunicorn_conf.py) | high | mitigate | `bind = "127.0.0.1:8000"` exact loopback value; verified locally by positive exact-match assertion (`g.bind == '127.0.0.1:8000'`), and confirmed live on the Pi (Plan 02) that the port is NOT reachable from another LAN device. No negative grep on an all-interfaces literal (positive assertion is stronger and avoids comment-text traps). |
| T-02-03 | Concurrency / Tampering | whitenoise static under multiple workers (STORAGES) | low | accept | CompressedManifestStaticFilesStorage compresses once at build-time (collectstatic, single-threaded); runtime workers only read immutable pre-built files — no concurrent-write contention exists by design. Rationale documented; no runtime mitigation needed. |
| T-02-SC | Tampering (supply chain) | pip installs (gunicorn, whitenoise) | high | mitigate | RESEARCH Package Legitimacy Audit cross-verified both (long PyPI history, official docs); both approved — SUS was a false positive from missing download telemetry, NOT a red flag. No [ASSUMED]/[SLOP] packages, so no blocking-human legitimacy checkpoint is required. Pins are exact (`==`). |
</threat_model>

<verification>
- Automated (Windows): `pip install -r requirements.txt`; `manage.py check`; MIDDLEWARE[1] + STORAGES backend assertions; `collectstatic --noinput` produces manifest + `.gz`; `py_compile` + bind/timeout assertions on gunicorn_conf.py.
- Deferred to Pi (Plan 02 checkpoint): gunicorn actually serving, static under gunicorn in a browser, loopback-only-reachability confirmed live, aarch64 install dry-run.
</verification>

<success_criteria>
- gunicorn and whitenoise are pinned + installed; WhiteNoiseMiddleware is index 1; STORAGES wires the whitenoise staticfiles backend; `collectstatic` builds a compressed manifest; gunicorn_conf.py binds loopback with a 60s+ timeout — all verified on this machine without ever running gunicorn.
</success_criteria>

<output>
Create `.planning/phases/02-gunicorn-systemd-process-supervision/02-01-SUMMARY.md` when done.
</output>
